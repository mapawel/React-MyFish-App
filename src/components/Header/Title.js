import React from 'react';
import styles from './Title.module.sass';

const Title = () => (
    <div className={styles.box}>
        <h1 className={styles.title}>my</h1>
        <h1 className={styles.title}>fish</h1>
    </div>
);
export default Title;